╔═══════════════════════════════════════════════════════════════════════╗
║                  OBLIRIM TESTING QUICK REFERENCE                      ║
╚═══════════════════════════════════════════════════════════════════════╝

┌─────────────────────────────────────────────────────────────────────┐
│ 🚀 QUICK START                                                      │
└─────────────────────────────────────────────────────────────────────┘

  ./quick-test.sh          Interactive menu with all test options
  
┌─────────────────────────────────────────────────────────────────────┐
│ 🧪 TESTING COMMANDS                                                 │
└─────────────────────────────────────────────────────────────────────┘

  WEB UI TESTING:
    ./test-dev.sh --full         Full test suite (syntax checks)
    ./test-dev.sh --server       Interactive test server (port 5001)
    ./launch.sh                  Development server (port 5000)

  TUI TESTING:
    ./test-tui.sh               ⭐ TUI with auto-backend (RECOMMENDED)
    ./launch-tui.sh              TUI with manual backend options

  COMBINED:
    ./quick-test.sh              Menu with all options

┌─────────────────────────────────────────────────────────────────────┐
│ 📊 TEST RESULTS FROM YOUR RUN                                       │
└─────────────────────────────────────────────────────────────────────┘

  ✅ Python Environment     - Working (Python 3.13.7)
  ✅ Dependencies           - All installed successfully
  ✅ Project Structure      - All files present
  ✅ Web Server (port 5001) - Responded successfully
  ✅ Web UI Pages           - Main page loaded
  ✅ API Endpoints          - Accessible
  ✅ Network Workflow       - Ethernet scan started
  
  ⚠️  TUI - Not tested yet (syntax check only in that test)

┌─────────────────────────────────────────────────────────────────────┐
│ 🎯 TO TEST TUI PROPERLY                                             │
└─────────────────────────────────────────────────────────────────────┘

  OPTION 1 (Easiest - Single Command):
    ./test-tui.sh
    
    This will:
    - Create/use virtual environment
    - Start backend on port 5001 if needed
    - Launch TUI interface
    - Auto-cleanup on exit

  OPTION 2 (Manual - Two Terminals):
    Terminal 1:  ./launch.sh          (starts backend)
    Terminal 2:  ./launch-tui.sh       (starts TUI)

  OPTION 3 (Quick Test Menu):
    ./quick-test.sh
    Choose option 4: "Test TUI with auto-backend"

┌─────────────────────────────────────────────────────────────────────┐
│ 🔧 TROUBLESHOOTING                                                  │
└─────────────────────────────────────────────────────────────────────┘

  TUI Won't Start:
    - Check terminal size (needs at least 80x24)
    - Verify TERM variable: echo $TERM
    - Test textual: python3 -c "import textual; print('OK')"

  Backend Not Running:
    - Check ports: sudo netstat -tlpn | grep :5000
    - Kill processes: pkill -f "python.*app.py"
    - Check logs: tail -f logs/dashboard.log

  Dependencies Missing:
    - Recreate venv: rm -rf .venv && python3 -m venv .venv
    - Reinstall: source .venv/bin/activate && pip install -r requirements.txt

┌─────────────────────────────────────────────────────────────────────┐
│ 📝 WHAT EACH TEST DOES                                              │
└─────────────────────────────────────────────────────────────────────┘

  test-dev.sh --full:
    ✓ Creates temp venv
    ✓ Installs dependencies
    ✓ Starts test server (port 5001)
    ✓ Tests API endpoints
    ✓ Validates TUI syntax (doesn't launch it)
    ✓ Tests network config
    ✓ Simulates security rules
    ✓ Auto-cleanup

  test-tui.sh:          ⭐ NEW - USE THIS FOR TUI
    ✓ Creates/uses .venv
    ✓ Starts backend on port 5001
    ✓ ACTUALLY LAUNCHES TUI INTERFACE
    ✓ Auto-cleanup on exit

  test-dev.sh --server:
    ✓ Starts test server
    ✓ Keeps running for manual testing
    ✓ Access at http://127.0.0.1:5001

┌─────────────────────────────────────────────────────────────────────┐
│ 🎨 TUI CONTROLS (when running)                                      │
└─────────────────────────────────────────────────────────────────────┘

  q       - Quit TUI
  r       - Refresh display
  s       - Start manual scan
  Ctrl+C  - Force exit

┌─────────────────────────────────────────────────────────────────────┐
│ ⚡ NEXT STEPS                                                        │
└─────────────────────────────────────────────────────────────────────┘

  1. Run:  ./test-tui.sh
  2. Verify TUI displays correctly
  3. Test TUI controls (q, r, s)
  4. If satisfied, run:  ./install.sh

═══════════════════════════════════════════════════════════════════════
